description "{{appName}} database"
start on filesystem and started docker
stop on runlevel [!2345]
respawn
env DOCKER_HOST=tcp://127.0.0.1:2375
exec /usr/bin/docker run \
  --rm=true \
  --name={{appName}}_db \
  --volume=/var/local/{{appName}}_db:/var/lib/postgresql/data \
  postgres:9.4
